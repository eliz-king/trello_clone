<h1><%= @task.id ? 'Edit Task' : 'Add Task' %></h1>

<%= form_with(model: [@list, @task], local: true) do |f| %>
  <%= f.label :task_name %>
  <%= f.text_field :task_name, required: true, autofocus: true %>

  <%= f.submit nil %>

<% end %>
